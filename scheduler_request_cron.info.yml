name: Scheduler Request Cron
description: Allows running Scheduler's lightweight as part of page requests.
package: Custom

type: module
core_version_requirement: ^8 || ^9
configure: scheduler_request_cron.settings
dependencies:
  - scheduler:scheduler
